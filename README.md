📌 API-сервис учёта инцидентов
Тестовое задание для Ucar <> TopDoer
Этот проект представляет собой небольшой сервис для учёта инцидентов (tasks/incidents), реализованный на FastAPI с использованием PostgreSQL, Alembic и Docker.

🚀 Стек технологий:

1. Язык программирования: Python
2. Фреймворк: FastAPI
3. Установщик пакетов и виртуальных окружений: UV
4. База данных: PostgreSQL
5. ORM: SQLAlchemy
6. Контейнеризация: Docker + Docker Compose

📁 Структура проекта:
.
├── app
│   ├── api
│   │   └── endpoints
│   ├── constants
│   ├── core
│   ├── crud
│   ├── models
│   └── schemas

Установка и Запуск:

1. Создать папку
2. Клонировать эту директорию из гита
3. Создать файл .env
4. И внести данные (Так-же есть пример файла .example_env):
   DB_HOST=localhost
   DB_PORT=5432
   DB_USER=user
   DB_PASS=super_pass
   DB_NAME=super_db_name
   
6. Собрать контейнер:
   docker-compose build --no-cache
   docker-compose up
   
8. Открыть браузер и вбить в поисковую строку: http://127.0.0.1:8000/docs
   Вы попадете в свагер фаст апи и можно проверить все апишки
